<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon="" class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="add a package first" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="nativescript-vueuse" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="nativescript-orientation" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="nativescript-keyboard" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="nativescript-clipboard" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>